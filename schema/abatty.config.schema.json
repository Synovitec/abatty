{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://abatty.io/schema/abatty.config.json",
  "title": "abatty.config.json",
  "description": "The one configuration of a repository under abatty: what the hooks trust (the gate command, the files, the push policy, the phases), the ratchet, the scrub, the provenance, the rules. At the repository root; the older place, .claude/adoption.json, is still read and the root file wins key by key.",
  "type": "object",
  "additionalProperties": false,
  "patternProperties": {
    "^\\$": {}
  },
  "properties": {
    "abatty": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+",
      "description": "The version of abatty this repository follows: written by init, moved by update, read by doctor. The one human-readable record of what was adopted; the lock under the agent folder carries the hashes."
    },
    "profiles": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "The profiles this repository follows, in order: a built-in id (synovitec), a file (./profiles/acme.mjs) or an installed package exporting a profile (rules, phases, presets, harness rule files as one package). [\"synovitec\"] when absent; a client project names only its own."
    },
    "workspaces": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "The preset of a workspace folder when its dependencies do not say (`\"apps/web\": \"next\"`). Each workspace with a preset is gated in its own folder by that preset's steps; the repository-level steps run once at the root."
    },
    "stage": {
      "type": "string",
      "enum": ["design", "build", "run"],
      "description": "Where the repository is: design (documents, decisions, a schema, a mockup; no application yet), build, or run (serving users, a deploy surface). Read from the tree when absent; a rule of another stage is n/a with the stage named."
    },
    "stack": {
      "type": "string",
      "description": "The preset this repository runs: next, vite-react, node."
    },
    "baseBranch": {
      "type": "string",
      "description": "The branch a night starts from and the Stop gate reads the config from."
    },
    "branchPrefix": {
      "type": "string",
      "description": "The night's branch is <prefix>-<date>."
    },
    "directPushToBase": {
      "type": "boolean",
      "description": "true for an internal platform that pushes to the base after the gate; false for a PR-only project. A night never pushes to the base."
    },
    "commands": {
      "type": "object",
      "description": "The repository's own commands, as the hooks and the runner call them.",
      "additionalProperties": {
        "type": "string"
      },
      "properties": {
        "gate": {
          "type": "string"
        },
        "gateFull": {
          "type": "string"
        },
        "standards": {
          "type": "string"
        },
        "lintFile": {
          "type": "string"
        },
        "test": {
          "type": "string"
        },
        "typecheck": {
          "type": "string"
        }
      }
    },
    "files": {
      "type": "object",
      "description": "Where the changelog, the version, the night's state, the decisions, the progress log and the baseline live.",
      "additionalProperties": {
        "type": "string"
      },
      "properties": {
        "changelog": {
          "type": "string"
        },
        "version": {
          "type": "string"
        },
        "state": {
          "type": "string"
        },
        "decisions": {
          "type": "string"
        },
        "progress": {
          "type": "string"
        },
        "baseline": {
          "type": "string"
        }
      }
    },
    "standard": {
      "type": "string",
      "description": "The standard's document, for the agents to read."
    },
    "plan": {
      "type": "string",
      "description": "The adoption plan, for the agents to read."
    },
    "sourceGlobs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "changelogRequiredFor": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Path prefixes whose commits must carry a changelog line."
    },
    "coupled": {
      "type": "array",
      "description": "Coupled paths: when this changes, that changes in the same push. Judged per commit over the pushed range by the ratchet (the gate, CI) and the Stop hook; a `when` path changed without a `then` path after it is refused until a later commit touches it. A schema and its migration, an API and its client, a document and the code it describes.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["when", "then"],
        "properties": {
          "when": {
            "type": ["string", "array"],
            "items": {
              "type": "string"
            },
            "description": "A prefix or a glob (* a segment, ** any depth)."
          },
          "then": {
            "type": ["string", "array"],
            "items": {
              "type": "string"
            }
          },
          "why": {
            "type": "string"
          }
        }
      }
    },
    "protectedPaths": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Paths a night never writes: applied migrations, env files, production compose."
    },
    "mcpServers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "The MCP servers a night may call; every other server's tools are denied."
    },
    "rules": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "properties": {
        "local": {
          "type": "string",
          "description": "The repository's own rules file (abatty.rules.mjs)."
        },
        "waived": {
          "type": "object",
          "description": "Rule ID to a reason, or to { reason, until }.",
          "additionalProperties": true
        }
      }
    },
    "ratchet": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "properties": {
        "local": {
          "type": "string",
          "description": "The repository's own probes file (abatty.probes.mjs)."
        },
        "include": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "exclude": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "enable": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The opt-in probes this repository runs (valid.unparsedBoundary, valid.wholeEnv, auth.unguardedPage, api.unboundedList, api.rowReturn, api.floatMoney, cache.serverCacheUse, fn.shapeExemptions)."
        },
        "boundedBy": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "api.unboundedList: the helpers that bound a list, beside a take."
        },
        "secretFields": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "api.rowReturn: words a column a select must never carry contains (default password, secret, token, otp, hash)."
        },
        "moneyFields": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "api.floatMoney: the field names that hold money (default price, amount, cost, fee, subtotal, balance)."
        },
        "pageGuards": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["pages", "guard"],
            "properties": {
              "pages": {
                "type": "string",
                "description": "A regex over the page paths this guard protects."
              },
              "guard": {
                "type": "string",
                "description": "The function a protected page must await first."
              }
            }
          },
          "description": "auth.unguardedPage: which pages must await which guard first."
        },
        "shapeList": {
          "type": "string",
          "description": "fn.shapeExemptions: a JSON file whose `files` array lists the files the shape rules are still off for."
        },
        "hard": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Metrics held at zero whatever the probe says."
        },
        "ratchet": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Metrics held as a ratchet even at zero (never promoted to hard)."
        },
        "mustScan": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "cap": {
          "type": "integer",
          "minimum": 1
        },
        "defaultMax": {
          "type": "integer",
          "minimum": 1
        },
        "contextMax": {
          "type": "integer",
          "minimum": 1
        },
        "barrelMax": {
          "type": "integer",
          "minimum": 0
        },
        "kinds": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "match": {
                "type": "string"
              },
              "kind": {
                "type": "string"
              },
              "max": {
                "type": "integer",
                "minimum": 1
              }
            },
            "required": ["match", "kind", "max"]
          }
        },
        "exempt": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "envModule": {
          "type": "string"
        },
        "citationsExempt": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "changelog": {
          "type": "string"
        },
        "changelogRequiredFor": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "scrub": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Off unless set: provenance is the default."
        },
        "allow": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Path substrings whose lines are not findings."
        },
        "map": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        }
      }
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "properties": {
        "trailer": {
          "type": "string",
          "description": "A disclosure line every unattended commit carries; empty for none."
        }
      }
    },
    "tenantKeys": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "The column names that scope a row to a tenant here, beside tenant_id, store_id, company_id and organisation_id (DATA-TENANT)."
    },
    "style": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "properties": {
        "emDash": {
          "enum": ["refused", "allowed"],
          "description": "Whether this repository's typography uses the em dash; allowed makes FLOW-EMDASH n/a (French sets it as punctuation)."
        }
      }
    },
    "secrets": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "properties": {
        "allow": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Path substrings the secret scan skips (fixtures, samples), with the reason beside them."
        }
      }
    },
    "security": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "description": "Security scoping the gate reads. The dependency audit is scoped before it is trusted: an unscoped audit is the one people switch off.",
      "properties": {
        "audit": {
          "type": "object",
          "additionalProperties": false,
          "patternProperties": {
            "^\\$": {}
          },
          "properties": {
            "level": {
              "type": "string",
              "enum": ["info", "low", "moderate", "high", "critical"],
              "description": "The severity floor the audit refuses at; below it an advisory is reported, never a red gate. Default high."
            },
            "allow": {
              "type": "array",
              "description": "Advisories this repository has decided to carry, each with a reason and, where the decision is not permanent, a date it stops applying. An allowance that has expired stops allowing and the gate says which one ran out.",
              "items": {
                "type": "object",
                "additionalProperties": false,
                "required": ["id", "reason"],
                "properties": {
                  "id": {
                    "type": "string",
                    "description": "The package name or the advisory id the allowance covers."
                  },
                  "reason": {
                    "type": "string",
                    "description": "Why this repository carries it: no fix available, not reachable from the shipped code, a pinned transitive."
                  },
                  "until": {
                    "type": "string",
                    "description": "The date the allowance stops applying (YYYY-MM-DD). Absent means permanent, which is a decision a reviewer can see."
                  }
                }
              }
            }
          }
        }
      }
    },
    "ci": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "properties": {
        "providers": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["woodpecker", "github"]
          },
          "description": "The CI providers abatty ci generates for, from the gate; init writes them too."
        }
      }
    },
    "lintOnEdit": {
      "type": "boolean"
    },
    "lintExtensions": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "maxStopBlocks": {
      "type": "integer",
      "minimum": 1,
      "maximum": 8
    },
    "maxSessionsPerPhase": {
      "type": "integer",
      "minimum": 1
    },
    "phases": {
      "type": "array",
      "items": {
        "type": ["integer", "string"]
      },
      "description": "The phases a night walks, in order."
    },
    "allowance": {
      "type": "object",
      "additionalProperties": false,
      "description": "What a night may spend, in the unit the account is billed in: dollars for metered access; sessions or tokens under a subscription, where the dollar figure is a proxy. Any cap reached ends the night. Without one, 60 USD.",
      "properties": {
        "usd": {
          "type": "number",
          "minimum": 0
        },
        "sessions": {
          "type": "integer",
          "minimum": 1
        },
        "tokens": {
          "type": "integer",
          "minimum": 1
        }
      }
    },
    "sandbox": {
      "type": "object",
      "additionalProperties": false,
      "description": "The OS boundary under the guard for a night: the tree writable, the harness, the root config and the protected paths read-only, the rest of the machine read-only. Built by the runner and proven by a probe before the first session.",
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["auto", "required", "off"],
          "description": "auto: a driver found on the machine, else the guard alone (said loudly); required: no driver, no night; off: never."
        },
        "driver": {
          "type": "string",
          "enum": ["bwrap", "sandbox-exec", "container"],
          "description": "Name a driver instead of detecting one: bubblewrap (Linux), sandbox-exec (macOS), a container image."
        },
        "command": {
          "type": "string",
          "description": "The driver's executable when it is not on the PATH (or the container engine: docker, podman)."
        },
        "image": {
          "type": "string",
          "description": "A container image carrying the agent and a Node; naming one selects the container driver."
        },
        "readOnly": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "More paths read-only inside the boundary."
        },
        "writable": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Paths outside the tree a session may write (a cache, a tool's state folder)."
        },
        "env": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The variables a container passes through, by name."
        },
        "home": {
          "type": "string",
          "description": "The container's HOME, where the agent's state folder is mounted (default /root)."
        }
      }
    },
    "preflight": {
      "type": "object",
      "additionalProperties": false,
      "patternProperties": {
        "^\\$": {}
      },
      "description": "The night's pre-flight: what is read before an agent is pointed at the tree.",
      "properties": {
        "trust": {
          "type": "boolean",
          "description": "Whether the trust scan runs before a night (default true). Setting it to false is a decision a reviewer can see, and the rule SEC-AGENT-TRUST reports it as partial rather than present."
        },
        "trustAllow": {
          "type": "array",
          "description": "Paths the trust scan skips, as regular expressions against the repository-relative path, each with the reason it is there. For a repository that keeps injection fixtures of its own. A pattern that does not compile skips nothing.",
          "items": {
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "additionalProperties": false,
                "required": ["path", "reason"],
                "properties": {
                  "path": {
                    "type": "string",
                    "description": "A regular expression against the repository-relative path."
                  },
                  "reason": {
                    "type": "string",
                    "description": "Why this path carries text the scan would otherwise stop a night for."
                  }
                }
              }
            ]
          }
        }
      }
    }
  }
}
