---
title: "Rule catalog"
description: "Every check abatty makes, as data: 79 rules in 15 families, 64 must and 15 should; once present, 47 are insured by a machine that refuses, 7 by a number that may only fall, 13 by the reviewer's checklist, 12 by nothing yet. Generated by abatty rules --md; the test keeps this file equal to the catalog."
category: reference
status: living
audience: ["developer", "architect", "reviewer"]
tags: ["rules", "catalog", "standards", "generated"]
related: ["./README.md", "./ROADMAP.md"]
---

# Rule catalog

79 rules in 15 families; 64 must, 15 should. A rule is a check with a reason: `abatty measure` runs every one, `abatty explain <ID>` opens one against a repository. **Insured by** says what holds the rule once it is present: **hard**, a machine refuses the work (a lint error, a hook denial, a failing gate step); **ratchet**, a number that may only fall, with a per-file floor; **review**, an item of the reviewer's checklist; **prose**, written and checked by nothing yet. A rule that carries a **ceiling** names the strongest level any machine could reach for it and why it stops there; once it is at that level it leaves the promotion queue instead of sitting in it forever. The standard's rule IDs are `FAMILY.N` (`CODE.6`), a namespace of their own that a repository's citation check never claims. **Applies** says where a rule concerns a repository at all, and at which stage (design, build, run): elsewhere its finding is n/a with the reason, never missing.

The rules come from profiles: the built-in `synovitec` profile is the standard the package was built on; a repository names the profiles it follows in its config (`profiles`), a file or a package exporting a profile of its own among them. A repository adds rules of its own in `abatty.rules.mjs` at its root (the same shape, `export const rules = [...]`) and waives one with a reason in its config (`rules.waived`); a waived rule is listed, not scored.

## Documents

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| DOC-CONTEXT | CLAUDE.md exists and stays under 200 lines | must | prose | A.1 / status | always | AIR.1 | The context file is read whole at the start of every session; past two hundred lines the model stops holding it and confidently applies the wrong half. The one repository that let it reach 1,124 lines is the motivating case. A file that is still the template, placeholders and all, is a description of a context file, not one. | Move domain sections to .claude/rules/<topic>.md with paths: front matter; keep §1-§10 of the template |
| DOC-CONTEXT-SECTIONS | CLAUDE.md carries delivery rules, skills table, autonomy contract, known gaps | must | prose | A.1 | always | AIR.1 | The five sections are what an agent needs and nothing else: the non-negotiables, the commands, how work is delivered, which protocol to follow, and what it may decide alone. A context file without them is a description, not an instruction. | Add the missing sections from the agent-context template §7-§10 |
| DOC-RULES | .claude/rules/*.md path-scoped rules for stack and domain conventions | should | prose | A.1 | always | AIR.1 | A rule scoped to paths loads only when the agent touches those files, which keeps the context file short and the domain reasoning where it applies. | Copy the applicable rules templates and scope them with paths: |
| DOC-CONVENTIONS | A CODE_CONVENTIONS.md under docs: how this stack applies the standard | should | prose (ceiling review: A machine can see the file and its headings. Whether it describes how THIS stack applies the standard, rather than repeating the standard, is a reading; a check that asserted it would be asserting that prose is good.) | A.1 | always | - | The standard is generic; each stack applies it with named choices (folder layout, the ORM, the test runner). Written once, they stop being re-decided in every session. | Write it from the standard §3-§7 for this stack |
| DOC-PROGRESS | A STANDARDS_PROGRESS.md under docs: numbers-only scoreboard and log | must | review | 0 | always | P.3 | Honest reporting is numbers before and after, dated; a log that says 'improved' hides regressions. The scoreboard is where the ratchet's readings and the nights' receipts land. | Create it with the scoreboard, phase table and dated log |
| DOC-CHANGELOG | CHANGELOG.md in Keep-a-Changelog shape with an [Unreleased] section | must | hard | 0 / 11 | always | CHANGE.1 | A change that is not written in the same push is lost to the next reader; the [Unreleased] section is where the gate checks that the push wrote it. | Add ## [Unreleased] with Added/Changed/Fixed groups; enforce the same-push rule in the gate |
| DOC-INDEX | docs index listing every document | must | hard | 11 | always | DOC.3 | A document nobody can find is a document nobody reads; the index is the one entry point, and a drift check keeps it equal to the tree. | Create a README.md under docs with one row per document |
| DOC-ADR | A decisions record (MADR entries or a decision log) | must | prose (ceiling review: A machine can count the entries and read their front matter. Whether the decisions that actually shaped the system are among them is knowable only to somebody who was there: the missing entry leaves no trace to scan for.) | A.1 | always | FLOW.4 | A decision without a record is re-litigated by the next person or the next session; the night's decision codes land in the same file. | Create a decisions folder under docs with 0001-*.md for the five day-0 decisions |
| DOC-FRONTMATTER | Every doc opens with front matter | must | hard | 11 | always | DOC.2 | The front matter (title, status, audience, related, last_verified) is what lets a machine list, date and index the documents; without it every doc check is a guess. | Add the standard's front matter to every doc and a check that fails without it |
| DOC-FRESHNESS | Docs carry last_verified and source_truth so freshness is measured by the diff | must | ratchet | 11 | always | DOC.5 | A doc that names the code it describes can be checked against the diff: when the code moved after the doc was verified, the doc is behind, and an agent reading it confidently does the wrong thing. | Add last_verified and source_truth; add a behind-code or freshness metric to the ratchet |
| DOC-AGENTS-MD | AGENTS.md imported by CLAUDE.md when other agents read the repo (optional) | should | prose | - | always | - | Two context files drift apart; when both exist, one imports the other so there is one source. | Start CLAUDE.md with @AGENTS.md if both must stay in sync |

## Instrument

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| INST-RATCHET | A standards ratchet script with a committed baseline | must | hard | 0 | always; at build, run | P.2 | A rule with a number is enforced the day the number is committed: existing debt is allowed, a new violation is refused, and a number that must go up is a decision written in the same commit. | Add `standards: abatty ratchet` (init writes it) and run `abatty baseline` to record the floor |
| INST-DEBT | Per-file floors (debt) beside the totals | must | hard | 0 | always; at build, run | P.2 | A total can hide a relocation: one file cleaned, another made worse, the sum unchanged. The floor per file refuses the new violation wherever it lands. | Record debt per file so a total cannot hide a relocation |
| INST-CONTROLS | Control cases both directions: for the probes, and for every gate step | must | hard | 0 | always; at build, run | P.1 | A probe that has never reported a planted violation may be reporting nothing, and a gate step that never went red may be checking nothing; a check with no failing control is not a check. | Run the ratchet from the package (its probes carry their controls; `abatty ratchet --controls` runs them), then `abatty doctor --controls`: it plants a violation per gate step and reports a step that stays green as absent |
| INST-GATE | One gate script called by the pre-push hook and by npm | must | hard | 0 | always; at build, run | FLOW.2 | One command that says green or red is what a hook, a CI step, an agent's stop and a human all run; two lists of checks drift apart. | Add a gate.mjs under scripts/ci and a .githooks/pre-push calling it (npm run hooks:install) |
| INST-PRECOMMIT | Pre-commit hook: console.log, secrets, locale set | must | hard | 0 | always | SEC.1, I18N.1 | The cheapest moment to refuse a secret or a stray console.log is before it is in a commit; after that it is in the history. | Add a staged-files pre-commit hook sharing the secret scanner with CI |
| INST-CI | CI with the same gates | must | hard | 0 | always | - | The hook runs on the machine that pushes and can be skipped there; CI re-runs every gate independently of who pushed and of what they skipped. A pipeline is credited for the scripts it can run, not for the words it names: one that names a script the package lacks is red or never ran, and a comment runs nothing. | abatty ci generates the pipeline from the gate (Woodpecker, GitHub Actions) |
| INST-CI-STEPS | CI runs lint, typecheck, tests, standards, secret scan, audit | must | hard | 0 | always; at build, run | CODE.4, CODE.3, TEST.1, P.2, SEC.1 | A CI that runs the tests but not the ratchet lets the numbers rise unseen; the six steps are the gate, no less. A step whose script the package does not have is named, not counted, and a comment that names a step is not a step. | Add the missing steps; a secret scan and an audit are one step each |
| INST-DEAD-CI | No dead CI workflow posting meaningless red checks | should | prose | 0 | always; at build, run | - | A red check nobody reads teaches everyone to ignore red checks. | Reduce GitHub workflows to workflow_dispatch or delete them |

## Harness

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| HARNESS-HOOKS | Guard, protect, stop-gate, check-direction, session-brief, self-test hooks present and wired | must | hard | A.1 / 0 | always | FLOW.2, FLOW.3, DATA.1 | Context is not enforcement: the guard denies the dangerous command before it runs, the stop gate refuses 'done' while the gate is red, protect keeps the harness read-only to the worker, and the direction check refuses a loosened floor. Prose in the context file does none of that. | Copy the hooks and settings.project.json (abatty init); run the self-test |
| HARNESS-ADOPTION | abatty.config.json (or .claude/adoption.json) names the gate, files, push policy and phases | must | hard | A.1 | always | - | The hooks read one config for the gate command, the protected paths, the base branch and the phases; without it every hook guesses. | abatty init writes abatty.config.json at the root; edit the commands there |
| HARNESS-SKILL | adopt-standards skill and standards-reviewer agent | must | review | A.1 | always | - | The skill is the protocol of a night (measure, pick the phase, ten files, gate, review, commit); the reviewer is the second reader with the thirteen-item checklist before every phase commit. | Copy the skill and the two agents (abatty init) |
| HARNESS-GITIGNORE | .claude/night/ ignored | must | hard | A.1 | always | - | The night's run state and receipts are machine-local; committed, they would be a trace and a merge conflict. | Add .claude/night/ to .gitignore |

## Code

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| CODE-LINTER | A linter configured, for every language in the tree | must | hard | 1 | a repository with sources (any language pack); at build, run | CODE.4 | The linter is where most rules of the standard become an error a machine refuses; without a config there is nothing to hold them. Each language pack names its linter; the rule keeps the same words. | Add the pack's linter config (the day-0 block from CODE.1: ESLint, oxlint or Biome for JavaScript; ruff for Python) |
| CODE-MAXWARN | lint runs with a warning as an error (--max-warnings=0, --error-on-warnings) | must | hard | 1 | a repository with JavaScript or TypeScript sources; at build, run | CODE.4 | A warning nobody has to fix is a rule nobody follows; at zero, a warning is an error with a softer name. | Make a warning fail the lint script: --max-warnings=0 (ESLint, oxlint) or --error-on-warnings (Biome) |
| CODE-SHAPE | Function shape held: max-lines, max-lines-per-function, complexity, max-params in the linter or the ratchet | must | hard | 7 / 8 | a repository with JavaScript or TypeScript sources; at build, run | CODE.1, CODE.2 | An 800-line file and a 150-line function are what an agent cannot read whole, and what a reviewer cannot hold; the four limits are the shape a module keeps. | Add the four rules at warn under --max-warnings=0, exemptions generated from the baseline debt |
| CODE-JSDOC | jsdoc/require-jsdoc publicOnly on the exported surface | must | hard | 11 | a repository with JavaScript or TypeScript sources; at build, run | CODE.7 | An export without a block is a contract nobody wrote down; the block says why it exists, which is the one thing the code cannot say. | Add eslint-plugin-jsdoc (typescript-flavor on TS) with the fixer disabled |
| CODE-ARCH-IMPORTS | no-restricted-imports / import boundaries hold the architecture | must | hard | 1 | a repository with JavaScript or TypeScript sources; at build, run | CODE.4, CODE.5 | A vendor SDK imported outside its provider home, or a component reaching the database, is the architecture eroding one import at a time; the linter refuses the import. | Ban vendor SDKs outside their provider home and enforce the import direction |
| CODE-ARCH-GRAPH | The import graph is checked: dependency-cruiser with no-circular, no-orphans and one rule per arrow of the boundary map, in the gate | must | hard | 12 | a repository with JavaScript or TypeScript sources; at build, run | CODE.5 | The boundary map in the context file is a wish until the graph is checked: a cycle, an orphan and a forbidden arrow are then refused by the gate, not hoped against. | Copy the dependency-cruiser template, write one rule per arrow of the boundary map, add the graph step to the gate; on an existing repository start from --baseline |
| CODE-DEADCODE | Dead code is a gate: files, dependencies, exports and types at zero issues | must | hard | 12 | a repository with sources (any language pack); at build, run | CODE.6 | Dead code is read, maintained and reasoned about for nothing; an unused dependency is an attack surface. knip at zero makes both a refusal instead of a cleanup. | Copy the knip template, add knip --max-issues 0 to the gate (start at today's count, ratchet dead.knipIssues to zero) |
| CODE-DUP | Duplication measured as a ratchet metric | should | ratchet | 12 | a repository with JavaScript or TypeScript sources; at build, run | CODE.12 | Two copies of a block fix one bug twice, or once; the count of clones is a number that may only fall. The practice is the number, not a tool: the package's own reading needs no dependency, and a repository's own detector counts as well. | Add code.clones to ratchet.enable (no dependency) and record the floor with abatty baseline, or read your own clone detector into the ratchet as a metric |
| CODE-SIZE-800 | No source file over the 800-line cap | must | hard | 8 | a repository with JavaScript or TypeScript sources; at build, run | CODE.1 | Past 800 lines a file has more than one responsibility, and no reader, human or model, holds it whole. | Split by responsibility; the ratchet holds the count |
| CODE-SIZE-300 | Source files over 300 code lines (the threshold where an agent stops reading a file whole) | should | ratchet | 8 | a repository with JavaScript or TypeScript sources; at build, run | CODE.1 | Three hundred code lines is where an agent starts reading a file in pieces and editing what it did not read; per-kind budgets keep modules under it. | Per-kind budgets in the ratchet, then split worst-first |
| CODE-BARRELS | No wide barrel files | must | ratchet | 8 | a repository with JavaScript or TypeScript sources; at build, run | CODE.5 | A wide barrel hides who imports what, defeats tree-shaking and makes every import a potential cycle; import from the defining file. | Import from the defining file; keep barrels leaf-level |
| CODE-FORMAT | A formatter configured and the format checked, for every language in the tree | must | hard | 1 | a repository with sources (any language pack); at build, run | CODE.4 | Formatting argued in review is attention taken from the change; one formatter, checked, ends the argument. | Add Prettier and a format check with --end-of-line auto in the gate |

## Types

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| TYPES-CHECKJS | A JavaScript repository typechecks over checkJs and ratchets the count | must | ratchet | 9 | a repository with JavaScript or TypeScript sources; at build, run | CODE.3 | A JavaScript repository still has types, in the JSDoc and in the shapes it passes around; checkJs reads them and the count of errors is a number that may only fall. | Add tsconfig with allowJs/checkJs and a typecheck script |
| TYPES-STRICT | strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes | must | hard | 9 | a repository with JavaScript or TypeScript sources; at build, run | CODE.3 | strict alone lets an index read return undefined unnoticed and an optional property be set to undefined; the two extra flags close the holes the runtime finds first. | Enable the missing flags; migrate in the order strictNullChecks, noImplicitAny, strict, noUncheckedIndexedAccess, exactOptionalPropertyTypes |
| TYPES-SCRIPT | A typecheck script runs the compiler without emitting, for every language in the tree | must | hard | 1 | a repository with sources (any language pack); at build, run | CODE.3 | The build may skip the type errors a bundler tolerates; tsc --noEmit is the one command that reads them all, and the gate needs its name. | Add typecheck: tsc --noEmit and put it in the gate |
| TYPES-ESCAPES | any and @ts-ignore absent from source | must | ratchet | 1 / 9 | a repository with JavaScript or TypeScript sources; at build, run | CODE.3 | Every any is a place the compiler was told to look away; the count is the honest measure of how strict the types are. | Ban any with no-explicit-any; ratchet the count |

## Boundaries

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| VALID-ZOD | A schema library validates every boundary | must | hard | 4 | a repository with a server, an API or a browser application; at build, run | VALID.1 | A request, a form, a webhook, an env: everything that crosses into the program is parsed once at the edge or trusted everywhere inside. The schema is the type the runtime checks. | Add zod and parse every request, form and env at the edge |
| VALID-ENV | One validated env module; no raw process.env elsewhere | must | ratchet | 4 | a repository with a server, an API or a browser application; at build, run | VALID.3 | A raw process.env read fails at the line that reads it, in production, at 3 a.m.; one module parsed at boot fails at start, on the developer's screen. | Create one env module (env.ts under src/lib or the server config folder) parsed at boot; ratchet valid.rawEnv to its documented floor |
| VALID-LOCAL-DAY | A calendar day comes from one function, derived from the clock it is compared to | must | hard | 4 | a repository with sources (any language pack); at build, run | VALID.5 | A date is not an instant: an instant is a point on a universal line, a date is a question about somebody's calendar. A day sliced off a UTC timestamp answers it for Greenwich and for nobody else, and compared against a local day - a commit date, a deadline, a date a person typed - it is wrong for the length of the machine's offset, once a day, every day. The suite does not see it because the pipeline runs at UTC. | Put the derivation in one function the whole repository calls, and pin a regression test to a zone whose calendar day differs from UTC's at the hour it runs |

## Data

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| DATA-MIGRATIONS | Migrations are the source of truth | must | hard | 0 | a repository with a database; at build, run | DATA.1 | A schema that lives only in the database cannot be reviewed, replayed or rolled back; an applied migration edited afterwards is a schema that differs per environment. | Generate or hand-write migrations; never edit an applied one |
| DATA-TENANT | Tenant isolation proven: RLS or a scoped service plus an isolation test | must | hard | 4 / 10 | a repository with a database; at build, run | DATA.3 | One tenant reading another's rows is the failure a multi-tenant product does not survive; it is proven by a negative test on a real database, not by the presence of a column. | Add a negative isolation integration test (tenant A cannot read B); a tenant column other than tenant_id, store_id, company_id or organisation_id is named in tenantKeys in the config |
| DATA-BACKUP | A restore drill exists | must | review (ceiling review: A machine can see that a drill exists and when it last ran. Whether a restore would actually come back is proven by running it against real data, which is a rehearsal a person schedules and watches, not a check a gate performs.) | - | a repository with a database; at run | DATA.5 | A backup that has never been restored is a hope; the drill, dated, is the proof, and a human runs it. | Add a restore drill script and date each run in OPERATIONS.md |

## Tests

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| TEST-UNIT | A unit test runner and tests, for every language in the tree | must | hard | 2 / 10 | a repository with sources (any language pack); at build, run | TEST.1 | A change without a test that goes red on the bug is a change nobody can verify; the runner is the first feedback loop an agent has. | Add a unit runner (vitest, jest or node --test) and colocated tests for services and guards |
| TEST-INTEGRATION | Integration tests against a real database | must | hard | 10 | a repository with a database; at build, run | TEST.2, DATA.4 | A mocked database proves the mock; constraints, row-level policies and transactions only fail on the real engine. | Stand up Testcontainers or the local dev DB; rolled-back transaction per test; no ORM mocking |
| TEST-COVERAGE | Coverage is gated on the change, with a floor under the tree | must | hard | 2 | a repository with sources (any language pack); at build, run | TEST.4 | A threshold on the tree total is the wrong question asked loudly: a whole new untested file passes while the total holds, and a refactor that deletes well-tested code fails for improving the codebase. What a reviewer wants to know is whether THIS change is tested, which is the coverage of the lines it touched. The total is still worth a floor, so coverage cannot drift down unnoticed; it is a floor, not the gate. | Gate on the coverage of the changed lines (a patch status, a diff-coverage step, or the runner's changed-files mode), keep a threshold on the total as a floor, and keep the report when the suite is red, which is when it is read |
| TEST-E2E | A browser suite with an accessibility scan on the same run | must | hard | 3 | a browser application; at build, run | TEST.3, A11Y.1 | The critical journeys are proven in a browser, and the accessibility violations axe can count are refused on the same run. | Add @axe-core/playwright on the critical journeys, blocking |
| TEST-E2E-CONFIG | retries CI-only and trace on-first-retry | should | review | 3 | a browser application; at build, run | TEST.3 | Retries on a developer's machine hide flakiness; a trace on the first retry is the evidence when CI fails. | Set retries: process.env.CI ? 2 : 0 and trace: 'on-first-retry' |
| TEST-MUTATION | Mutation testing wired, bounded to the change and to what a mutant can prove | should | hard | 10 | a repository with sources (any language pack); at build, run | TEST.5 | A test that passes when the code is broken proves nothing; the mutation score is the measure of the tests, not of the code. Unbounded, it is also the slowest check anybody has ever switched off: it mutates the whole tree on every run, and it mutates nodes no test could ever observe - a log line, a message string, a piece of code with no behaviour behind it - so it reports survivors nobody can kill and a run nobody waits for. | Bound it twice before you trust it: mutate what the change touched (--since / --incremental / a diff-driven glob) and ignore the nodes a mutant cannot prove anything about (arid nodes, excluded mutators, ignore patterns), with a floor it breaks at |

## Security

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| SEC-SECRETS | Secret scan in the hook and in CI from one config | must | hard | 0 | always | SEC.1 | A secret in the history is a secret to rotate; the hook refuses it before the commit, CI catches what the hook was skipped for, one config so the two agree. What the scan is worth is a measurement, not an assertion: `abatty secrets --benchmark` scores it against a published corpus, and the numbers are in docs/SECRET_SCAN_BENCHMARK.md. | abatty init writes the pre-commit hook (abatty secrets --staged) and abatty ci the CI step; the gate runs the same scan |
| SEC-DISCLOSURE | A coordinated vulnerability disclosure policy, where a reporter looks for it | should | prose (ceiling review: A machine can see that the policy exists, that it names a contact and that it says how long a reporter should expect to wait. Whether anybody answers that contact is the only thing that matters about it, and no check can see an unanswered mail.) | 0 | always | SEC.1 | Somebody who finds a vulnerability in your product will spend about five minutes looking for where to send it. With no policy they either post it publicly or drop it, and both are worse for you than an email. The file also carries the promise a reporter is owed: who reads it, and how long they should expect to wait. | Publish SECURITY.md at the root, in .github/ or in docs/, naming a contact and the response time a reporter should expect |
| SEC-AUDIT | Dependency audit in CI | must | hard | 0 | a repository with a package manifest; at build, run | SEC.1 | The supply chain is part of the product; a known vulnerability in a dependency is refused by the audit, not discovered by an incident. An UNSCOPED audit is the reason teams switch audits off: it reports a dev-only advisory nobody ships, at a severity nobody would act on, with no fix available, on every push, until somebody adds the flag that kills it for good. | Scope the audit before you trust it: production dependencies only (--omit=dev / --prod), a severity floor (--audit-level), and an advisory allowed with a reason and an expiry date (security.audit.allow) rather than a flag that silences everything |
| SEC-LOCKFILE | Lockfile committed and frozen installs in CI | must | hard | 0 | a repository with a package manifest; at build, run | SEC.1 | An install that resolves versions at build time builds a different product each time; the lockfile, frozen, is the one that was tested. | Use npm ci / pnpm install --frozen-lockfile in CI |
| SEC-ENVFILES | No .env file tracked in git | must | hard | 0 | always | SEC.1 | A tracked env file is every secret in it, in the history, on every clone. | Remove from the index, rotate the secrets, gitignore the pattern |

## Delivery

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| FLOW-COMMITS | Conventional Commits | must | review | 1 | always | FLOW.1 | A commit subject with a type and a scope is a changelog line and a filter; without the format the history is prose nobody searches. | Adopt the format; a commit-msg hook can refuse the rest |
| FLOW-TRAILER | No authorship trailer, where the repository opted into the scrub | must | hard | - | always | FLOW.1 | Provenance is the default: an agent's trailer on the commits it made is the audit trail. A repository that opted into the scrub (white-label work, scrub.enabled) keeps its history free of the name, and the commit-msg hook refuses the line before it lands. | Set attribution.commit to "" in the user settings; install the commit-msg hook (abatty scrub --message) |
| FLOW-EMDASH | No em-dash in code, copy or docs | must | hard | 1 | always | FLOW.1 | The character is the signature of generated text and a locale problem in copy; a hyphen says the same. | Replace with a hyphen; add the ratchet metric i18n.emDashInCopy |
| FLOW-CHANGELOG-GATE | Changelog-touched check over the pushed range | must | hard | 0 | always | CHANGE.1 | The rule 'changelog in the same push' is prose until the gate reads the pushed range and refuses a source change without its line. | Add changelog.missing to the ratchet with --range auto |
| FLOW-VERSION | A version the release process bumps (SemVer) | should | prose | - | always | - | A release without a number cannot be named in a bug report or pinned by a consumer. | Keep a SemVer version and cut [Unreleased] into a dated section on release |

## i18n

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| I18N-CATALOGUE | Translation catalogues exist | should | prose | - | a browser application, or a repository with translation catalogues; at build, run | I18N.1 | Copy in a catalogue can be translated, reviewed and checked for completeness; copy in the components can only be found by reading them. | - |
| I18N-LINT | Hardcoded component text is a lint error | must | hard | 1 | a browser application, or a repository with translation catalogues; at build, run | I18N.1 | One string typed into a component is one string the translators never see; the linter refuses it at the line. | Add react/jsx-no-literals or eslint-plugin-i18next at error |
| I18N-PARITY | Locale completeness checked (every key in every locale) | must | hard | 1 | a browser application, or a repository with translation catalogues; at build, run | I18N.1 | A key missing in one locale is a raw identifier on a customer's screen; a test over the set refuses the commit that forgot one. | Add a completeness test and the pre-commit locale-set check |

## a11y

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| A11Y-LINT | eslint-plugin-jsx-a11y at error, with component mapping when a component library is used | must | hard | 3 | a browser application; at build, run | A11Y.1 | The countable part of WCAG (a label, an alt, a role) is refused by the linter at the line; a component library needs the mapping or the plugin sees nothing. | Add the plugin at error and map the component library (settings + polymorphicPropName) |
| A11Y-CONTRAST | Contrast computed from the token file by a script | should | hard | 3 | a browser application; at build, run | A11Y.1 | Contrast is arithmetic over the tokens, both themes; a script computes what a reviewer estimates. | Add a contrast script over the token file, both themes |

## PWA

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| PWA-CONTRACT | Service worker under a contract test; assets resolve | must | hard | - | a repository with a service worker or a web manifest; at build, run | PWA.1 | A service worker that serves a stale shell serves it to every user until they clear the site; the contract test is the one thing that catches it before them. | Add the never-stale-shell contract test and the broken-asset metric |

## Observability

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| OBS-STRUCTURED | A structured logger, not lines on stdout | must | prose | 13 | a repository with a server; at build, run | OBS.1 | A line of text is searchable by a human and by nothing else; a record with fields is queryable, and the first incident is the wrong moment to discover which of the two the service writes. | Add a structured logger and make it the only writer of output from the server |
| OBS-REDACTION | Redaction by field path at the logger, never at the call sites | must | review (ceiling review: A machine can see that the logger redacts by field path and can list the paths. Whether that list names every field that would hurt if it were logged is a judgement about this system's data, and the field nobody thought of is invisible to a scan.) | 13 | a repository with a server; at build, run | OBS.1 | A field redacted where it is logged is redacted in the one place somebody remembered; a field redacted in the logger's configuration is redacted everywhere, including in the call added next week by somebody who never read this rule. | Configure redaction by field path at the logger (password, token, authorization, email, iban) and remove the hand-written masking at call sites |
| OBS-CONSOLE | Bare console output from the server is a lint error | must | hard | 13 | a repository with a server; at build, run | OBS.1 | One console call is the hole every structured-logging rule leaks through: it bypasses the redaction, the request scope and the transport, and it is the easiest thing in the world to add in a hurry. | Add no-console at error for the server's paths in the lint configuration |
| OBS-SIGTERM | SIGTERM drains the work in flight; it never exits on the spot | must | review (ceiling review: A machine can see that a handler is registered and that the server is asked to close. Whether the work in flight actually finishes before the process goes is proven by sending the signal under load and watching, which is a drill, not a scan.) | 13 | a repository with a server; at build, run | OBS.1 | Every deploy sends SIGTERM. A process that exits on the signal drops whatever it was holding, and the requests it drops are invisible because the thing that would have logged them is the thing that exited. | Handle SIGTERM: fail the health check, stop accepting work, drain what is in flight, then exit |
| OBS-HEALTH | A health endpoint the deploy and the load balancer can read | must | review | 13 | a repository with a server; at build, run | OBS.1 | Without one, the only signal that a service is alive is that it has not yet failed visibly, and a dead background worker looks exactly like an idle one. | Add a health or readiness endpoint, and make the SIGTERM handler fail it first |
| OBS-TRACKER | The error tracker is configured from the environment | should | prose | 13 | a repository with a server; at build, run | OBS.1 | The tracker has to work when the database is the broken thing, so its configuration cannot come from the database: a tracker that reads its own settings from the store it is meant to report on goes quiet exactly when it is needed. | Add an error tracker and read its configuration from the environment |

## Agent security

| ID | Rule | Level | Insured by | Phase | Applies | Standard | Why | Next step |
|---|---|---|---|---|---|---|---|---|
| SEC-AGENT-SANDBOX | An unattended run is sandboxed, and the sandbox proves its own boundary | must | review | 0 | a repository with the agent harness installed | SEC.7 | A sandbox nobody has watched hold is a claim, not a boundary. The cost of finding out it was decorative is a repository edited by something that was never supposed to reach it, at three in the morning, with nobody watching. | Run the night under a sandbox whose probe writes to a path it must not reach, and refuse the night when the boundary is present but does not hold |
| SEC-AGENT-PERMISSIONS | The permission surface is written down, and the dangerous commands are denied | must | hard | 0 | a repository with the agent harness installed | SEC.7 | A permission surface that is not written down is whatever the tool defaulted to on the day it was installed, and nobody on the team can tell you what it is. The deny list is the one place a reviewer can read what the agent may never do. | Declare the permission surface in the agent's settings, with the force push, the history rewrite and the hook bypass denied |
| SEC-AGENT-TRUST | The repository's own text is read before a model is pointed at it | must | review | 0 | a repository with the agent harness installed | SEC.7 | Text in a file an agent reads is an instruction in exactly the way a prompt is, and a repository is a place other people can write. The sandbox answers what the agent may do; nothing answers what the repository is telling it to do. | Run the night's pre-flight, which refuses to start when the tree carries instruction-shaped text, a command a document asks somebody to run, or a manifest that runs code at install time |
| SEC-AGENT-MCP | The servers a night may reach are named, and only those | must | review | 0 | a repository with the agent harness installed | SEC.7 | A server the configuration does not name is a surface nobody reviewed, reachable by something running with no human in the loop. The canary judges the declared set; an undeclared one is the case it cannot judge. | Name the MCP servers the night may use in the config, and let the canary refuse an undeclared one |
| SEC-AGENT-SHIM | The bypass is refused outside the agent as well as inside it | should | review | 0 | a repository with the agent harness installed | SEC.7 | A guard that lives in one tool's hook is a property of that tool, not of the repository: the same force push goes through from a second terminal, a script or another assistant. The layer that makes it a property of the repository is a `git` the shell finds first. | Install the shim (`abatty init` writes .claude/bin/) and put that directory on PATH for any run that is not being watched |
| SEC-AGENT-BYPASS | A commit made with a bypass is visible, not invisible | should | review | 0 | a repository with the agent harness installed | SEC.7 | The guard refuses a bypass at the moment it is attempted, which leaves the one that happened outside the guard: a commit made where the hook was not installed. A bypass that nobody can see afterwards is a gate with a hole nobody can measure. | Have CI report commits whose hook did not run, and accept the ones that carry a documented reason |
